{
  "name": "@office/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Office canonical contracts (OFF-002): provider-neutral branded IDs, tenant/project scope types, command envelope, domain event envelope with causality, pagination, API error envelope, and fail-closed schema versioning. The innermost shared kernel of the workspace: zero dependencies, imports nothing.",
  "sideEffects": false,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  }
}
