{
  // Placeholder TypeScript config for the Office web client (OFF-001).
  // Extends the workspace base so editors already apply strict settings here.
  // `src/` does not exist yet: application sources arrive with OFF-030.
  // The root `tsconfig.json` spans workspace sources for `pnpm typecheck`.
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": ["src"]
}
